import { CreditCard } from 'lucide-react';
import { PlaceholderPage } from './PlaceholderPage';

export function PaymentsPage() {
  return <PlaceholderPage title="Pagos" subtitle="Historial y gestión de pagos" icon={CreditCard} />;
}
