import { ShoppingCart } from 'lucide-react';
import { PlaceholderPage } from './PlaceholderPage';

export function OrdersPage() {
  return (
    <PlaceholderPage title="Pedidos" subtitle="Listado y gestión de pedidos" icon={ShoppingCart} />
  );
}
