import { UserCog } from 'lucide-react';
import { PlaceholderPage } from './PlaceholderPage';

export function CustomersPage() {
  return (
    <PlaceholderPage title="Clientes" subtitle="Gestión de clientes del ecommerce" icon={UserCog} />
  );
}
