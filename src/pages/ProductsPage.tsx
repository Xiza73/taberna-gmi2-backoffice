import { Package } from 'lucide-react';
import { PlaceholderPage } from './PlaceholderPage';

export function ProductsPage() {
  return (
    <PlaceholderPage title="Productos" subtitle="Gestión del catálogo de productos" icon={Package} />
  );
}
