import { Tag } from 'lucide-react';
import { PlaceholderPage } from './PlaceholderPage';

export function BrandsPage() {
  return <PlaceholderPage title="Marcas" subtitle="Gestión de marcas del catálogo" icon={Tag} />;
}
