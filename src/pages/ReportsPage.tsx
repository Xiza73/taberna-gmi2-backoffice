import { FileText } from 'lucide-react';
import { PlaceholderPage } from './PlaceholderPage';

export function ReportsPage() {
  return (
    <PlaceholderPage title="Reportes" subtitle="Generación de reportes" icon={FileText} />
  );
}
