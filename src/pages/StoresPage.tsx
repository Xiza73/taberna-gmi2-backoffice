import { Store } from 'lucide-react';
import { PlaceholderPage } from './PlaceholderPage';

export function StoresPage() {
  return (
    <PlaceholderPage title="Tiendas" subtitle="Configuración de tiendas" icon={Store} />
  );
}
