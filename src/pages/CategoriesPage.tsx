import { FolderTree } from 'lucide-react';
import { PlaceholderPage } from './PlaceholderPage';

export function CategoriesPage() {
  return (
    <PlaceholderPage
      title="Categorías"
      subtitle="Organización del catálogo por categorías"
      icon={FolderTree}
    />
  );
}
